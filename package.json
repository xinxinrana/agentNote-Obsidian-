{
  "name": "agentnote-obsidian",
  "version": "0.2.4",
  "description": "agentNote: a local note and file relay for AI agents, as an Obsidian plugin",
  "author": "Evan",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "test": "npm run build && node test/e2e.mjs && powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\\deploy-test.ps1",
    "typecheck": "tsc --noEmit",
    "deploy:test": "npm run build && powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\\deploy-test.ps1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "obsidian": "^1.7.2",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
